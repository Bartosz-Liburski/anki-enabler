SUPABASE_URL=###
SUPABASE_KEY=###
# Test-only: used exclusively by src/test/supabase-test-users.ts to seed/sign in/delete real
# test users against the hosted project for integration tests. Never read by app code — do not
# wire this into astro.config.mjs's env schema.
SUPABASE_SERVICE_ROLE_KEY=###